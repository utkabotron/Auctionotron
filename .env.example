# Copy to .env and adjust values
# Note: when running via python main.py, .env is NOT auto-loaded.
# Either set these in PowerShell (see instructions) or add python-dotenv loading in code.

DATABASE_URL=sqlite:///auction.db
SESSION_SECRET=change-me
TELEGRAM_BOT_TOKEN=replace-with-your-telegram-bot-token
DISABLE_TELEGRAM_AUTH=true
